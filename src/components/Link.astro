---
const { link } = Astro.props;

const linkIsUrl = link?.linktype === "url";
---

<a
  class="w-full h-full"
  href={linkIsUrl ? link?.cached_url : null}
  target={linkIsUrl ? link?.target : "_self"}
>
  <slot />
</a>
